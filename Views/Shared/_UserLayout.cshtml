<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css">
    <title>USER</title>
</head>
<body>
    @RenderBody()
    <div id="test"></div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js" integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.6.3.js"></script>
    
    <script>

        // đổ dữ liệu cữ ra trang edit user
        $(document).ready(function(){

            $(".Name").attr("value", sessionStorage.getItem('Name'));
            $(".PhoneNumber").attr("value", sessionStorage.getItem('Phonenumber'));
            $(".Address").attr("value", sessionStorage.getItem('Address'));
        })


    //đăng nhập gửi form lên user/xulydangnhap
    $('.btn').click(function(){
        //event.preventDefault();
        var Phone = $('#exampleInputEmail1').val()
        var Pass = $('#exampleInputPassword1').val()

        //sử lý ajax
        $.get("http://localhost:5246/User/XuLyLogin",
        { 
            Phone : Phone,
            Pass : Pass
            //http://localhost:5246/User/XuLyLogin?phone=..&pass=..
        }, function(data){
            //controller trả về một mảng thông tin user
          var array = data
          @* console.log(array[1]) *@
          // gán thông tin user vào session bằng js
            sessionStorage.setItem('Id', array[0])
            sessionStorage.setItem('Name', array[1])
            sessionStorage.setItem('Address', array[2])
            sessionStorage.setItem('Phonenumber', array[3])
            sessionStorage.setItem('Role', array[4])
        })

        $.post("dương dan url", {
            phone : 1,
             pass : 2},
              function(data){

        })

    })
    
    </script>
</body>
</html>